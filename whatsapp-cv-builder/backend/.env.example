# MongoDB Atlas Configuration
MONGODB_URI=mongodb+srv://thabang23mthimkulu_db_user:M84jk3NrN87v44Gw@cluster0.tzxuefz.mongodb.net/mydatabase
DB_NAME=career_pilot

# LLM Providers (configure at least one for live AI responses)
OPENAI_API_KEY=your-openai-api-key-here
GEMINI_API_KEY=your-gemini-api-key-here

# Server
PORT=8000
HOST=0.0.0.0

# JWT Security (REQUIRED in production — use a long, random secret)
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=replace-this-with-a-secure-random-string-in-production
